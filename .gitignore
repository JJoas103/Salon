# Maven
/target/

# IDE
/.idea/
/.vscode/
*.iml
/.settings/
/.classpath
/.project
/bin/

# npm — 이 프로젝트는 node 를 쓰지 않는다.
# 파이썬 FastAPI 를 설치하려다 npm 의 동명이인 패키지(fastapi)를 받은 흔적이 있어 통째로 막아둔다.
# 나중에 실제로 node 도구를 도입하면 package.json 은 이 목록에서 빼고 커밋할 것.
/node_modules/
/package.json
/package-lock.json

# OS
Thumbs.db
.DS_Store

# Logs
*.log

# 업로드된 파일(런타임 생성물) — 소스가 아니므로 커밋하지 않는다
/upload/

# sh
deploy.sh

# docs todo
docs/login-todo.md

# 참고: DB 계정을 팀에서 공유하는 개발 단계이므로
# app.properties 는 의도적으로 커밋한다. 운영 배포 시에는 분리할 것.

db.properties

# 결제 시크릿 키는 app.properties 의 카카오맵 키와 성격이 다르다.
# 지도 키는 어차피 브라우저에 노출되지만 결제 키는 서버에서만 쓰는 값이라
# 유출되면 우리 가맹점 이름으로 결제 요청을 만들 수 있다. 각자 로컬에만 둔다.
# 새로 받았다면 payment.properties.example 를 복사해서 채워 쓸 것.
payment.properties
docs/checkout-handoff.md
docs/checkout-todo.md
sql/migration_2026-08-04_consolidated.sql

# ai-service 이식 작업용 개인 메모(강사 실습본 참조 내용 포함, 팀 공유 문서 아님)
docs/ai-chatbot-todo.md

# ai-service 추가 아이디어 메모 — 아직 결정된 계획 아니라 팀 공유 문서 아님
docs/ai-service-ideas.txt

# 강의자료 zip 이 병합 중에 딸려 들어온 적이 있어 통째로 막아둔다.
# (그 안에 강사 계정 OpenAI 키가 들어있었다)
*.zip

# ai-service 의 llm.py 는 저장소 루트 .env 도 읽는다.
# ai-service/.gitignore 는 그 폴더 안만 막으므로 여기에도 규칙이 필요하다.
.env
.env.*
!.env.example
